{
  "name": "phantom-forest",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "moon build --target wasm --release && cp _build/wasm/release/build/main/main.wasm www/main.wasm && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6"
  }
}
